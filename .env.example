# Hugging Face token (https://huggingface.co/settings/tokens, type "Write") — needed for Task 1 follow
HF_TOKEN=
# Groq API key (https://console.groq.com/keys) — needed for `rag.py ask` and the web UI
GROQ_API_KEY=
# Optional: any Groq-served model id
LLM_MODEL=openai/gpt-oss-120b
